@import "tailwindcss";

.cart-page .qty-btn {
    width: 32px;
    height: 32px;
    background: var(--muted);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: 900;
    transition: all 0.2s;
}

.cart-page .qty-btn:hover {
    background: var(--primary);
    color: white;
}